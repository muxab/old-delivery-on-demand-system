export enum JourneyStatus {
    NEW = 'new',
    CONFIRMED = 'ready',
    PICKING = 'picking',
    ON_VEHICLE = 'onway',
    ARRIVED = 'arrived',
    DECLINED = 'reject',
    CANCELED = 'canceled',
  }
  